Flask Server for RainStorm API

## Use ##
using pyenv

run `python3 -m venv venv`
activate venv `source venv/bin/activate`
install dependencies `pip install -r requirements.txt`

run server with `run_server.sh`
